using UnityEngine;

public class testKan : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        //Hello
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
